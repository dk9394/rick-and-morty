import { FilterbyOriginPipe } from './filterby-origin.pipe';

describe('FilterbyOriginPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyOriginPipe();
    expect(pipe).toBeTruthy();
  });
});
