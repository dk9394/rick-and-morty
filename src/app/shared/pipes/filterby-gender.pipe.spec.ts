import { FilterbyGenderPipe } from './filterby-gender.pipe';

describe('FilterbyGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbyGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
