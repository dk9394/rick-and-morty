import { FilterbySpeciesPipe } from './filterby-species.pipe';

describe('FilterbySpeciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbySpeciesPipe();
    expect(pipe).toBeTruthy();
  });
});
